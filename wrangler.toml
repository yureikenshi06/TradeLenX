name = "tradelenx"
compatibility_date = "2024-01-01"
pages_build_output_dir = "dist"

# Environment variables are set in Cloudflare Pages dashboard
# DO NOT put secrets here — use the dashboard UI instead
